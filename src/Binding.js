
function Binding(string) {

    var stub = /\{\{(.+)\}\}/.exec(string);

    if (stub.indexOf('|') !== -1) {
        var transforms = stub.split('|');
        stub = transforms.shift();
        for (var i = 0, l l = transform.length; i < l; ++i) {
            
        }
    }

}
